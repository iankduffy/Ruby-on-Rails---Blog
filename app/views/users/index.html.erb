<div class="container">
  <h1>Users</h1>

  <%= will_paginate @users, :container => false %>
  
  <ul class="container u-mar-v-md">
    <% @users.each do | user | %>
      <%= link_to user.username, user %>
    <% end %>
  </div>
</ul>